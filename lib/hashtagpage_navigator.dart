import 'package:flutter/material.dart';

class HashtagPageNavigator extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Hashtags'),),
    body: Container(child: Text('Burası Hashtag Sayfası'),),);
      
    
  }
}